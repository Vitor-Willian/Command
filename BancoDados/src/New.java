public class New implements Command {

    /*
    public Object execute(Object args) {
        Data data = (Data) args;
        int id = data.getArg(0);
        String nome = data.getArg(1);
        Pessoa pessoa = new Pessoa(id, nome);
        return pessoa;
    }
    */
    
}
